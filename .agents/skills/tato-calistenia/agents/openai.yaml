interface:
  display_name: "Tato Calistenia"
  short_description: "Setter de Instagram y briefs de llamada; cola de leads para VALKA"
  default_prompt: "Usá $tato-calistenia para responder un chat con un solo DM, revisar un lote conocido sin enviar, o generar un brief si lo pido explícitamente."
policy:
  allow_implicit_invocation: true
